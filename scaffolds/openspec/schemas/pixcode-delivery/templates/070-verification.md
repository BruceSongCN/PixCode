# <模块中文名>｜<功能中文名>｜验证与交付结论

| 属性 | 内容 |
| --- | --- |
| Change | `<change-id>` |
| Capability | `<module-id>-<feature-id>` |
| Target | `<target-id>` |
| 验证状态 | 待执行 |
| 交付决定 | 待验收 |

## 执行环境声明

| 项目 | 内容 |
| --- | --- |
| 执行模式 / 配置来源 | `local / remote` / `<source>` |
| 验证 Profile / 数据库隔离 | `<profile>` / `<none / dedicated-container / shared-instance>` |
| 主机 / 工作区 / Runtime | `<host>` / `<workspace>` / `<runtime>` |
| 实际服务入口 | `<URL 或不适用及依据>` |
| 部署标识 / 版本 | `<镜像、提交、版本或不适用及依据>` |
| 契约 / 构建指纹 | `<OpenAPI 哈希、构建哈希或其他可复核标识>` |

## 执行摘要

| 阶段 | Target / 范围 | 环境 | 命令或入口 | 耗时 | 结果 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| Unit / Integration / Deploy / Smoke / Regression / Performance | `<target-id>` | <环境> | `<command>` | `<时长>` | 通过 / 失败 / 未执行 | <相对路径或链接> |

## 定向重跑与完整回归

<!-- 列出失败用例、case/tag/from-case 定向重跑结果，以及最后一次完整回归。不得用多次完整回归代替定位。 -->

## Requirement 与 Scenario 覆盖

| Requirement / Scenario | 结果 | 证据 |
| --- | --- | --- |
| `<引用>` | 通过 / 失败 / 未执行 | <证据> |

## 失败、未执行与遗留风险

<!-- 必须如实记录原因、影响和后续动作；没有则写“无”。 -->

## 验收范围与偏差

| 验收项 | 结果 | 依据 | 偏差处理 |
| --- | --- | --- | --- |
| <验收项> | 通过 / 不通过 / 未验收 | <证据> | <决定> |

## 最终交付决定

<!-- 待验收 / 通过 / 有条件通过 / 不通过。只有真实验证和验收后才能填写通过。 -->

## 交接说明

<!-- 发布顺序、未决事项、回退限制和接收方需要继续处理的内容。 -->
