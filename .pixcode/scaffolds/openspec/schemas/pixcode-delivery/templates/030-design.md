# <模块中文名>｜<功能中文名>｜设计评审稿

| 属性 | 内容 |
| --- | --- |
| 模块 | <模块中文名>（`<module-id>`） |
| 功能 | <功能中文名>（`<feature-id>`） |
| Change | `<change-id>` |
| Capability | `<module-id>-<feature-id>` |
| Target | `<target-id>` |

## 设计目标与边界

<!-- 说明需要满足的 Requirement、设计目标、系统边界和明确不处理的内容。 -->

## 功能设计

### 功能分解

| 功能单元 | 职责 | 所属 Target | 关联 Requirement |
| --- | --- | --- | --- |
| `<name>` | <职责> | `<target-id>` | `<引用>` |

### 总体方案

<!-- 描述组件关系、信息流和关键设计决策；复杂方案建议使用 Mermaid。 -->

## 流程设计

### 适用性

<!-- 适用 / 不适用；不适用时说明依据。 -->

### 参与者与主流程

<!-- 标明参与者、步骤所属 Target、跨 Target 边界；建议使用 Mermaid。 -->

### 分支、异常与补偿

| 触发条件 | 系统行为 | 恢复或补偿 |
| --- | --- | --- |
| <条件> | <行为> | <处理方式> |

### 状态迁移

<!-- 无状态变化时写“无”。 -->

## 模型适用性

<!-- 适用 / 不适用；不适用时说明依据。 -->

## 模型变化

| 类型 | 名称 | 变化 | 所属 Target |
| --- | --- | --- | --- |
| 实体 / 聚合根 / 值对象 / 枚举 / DTO | `<name>` | <新增或修改内容> | `<target-id>` |

## 实体字段定义

### `<EntityName>`（<实体中文名>）

| 字段 | 类型 | 空值 | 默认长度 | 中文含义/XML 字段说明 | 来源/规则 |
| --- | --- | --- | --- | --- | --- |
| `Id` | `Guid` | 否 | - | <实体中文名>主键 | 系统生成 |

<!--
每个新增持久化实体使用独立字段表并列出完整业务字段；既有实体只列本轮增量。
每行只能定义一个字段。字符串长度写项目常量及数值；无法确认时标记待确认。
没有模型变化时保留“模型适用性”，并删除示例实体表，避免被误认为真实设计。
-->

## 关系、不变量与持久化

<!-- 关系、状态、不变量、索引、迁移、兼容和数据修复影响。 -->

## API与共享契约

### 适用性

<!-- 适用 / 不适用；不适用时说明依据。 -->

### 契约清单

| 契约 | 提供方 Target | 消费方 Target | 变化类型 |
| --- | --- | --- | --- |
| `<API / Event / Message>` | `<target-id>` | `<target-id>` | 新增 / 兼容修改 / 破坏性修改 |

### 请求、响应与错误语义

<!-- 只记录评审所需稳定语义，不机械复制可生成的 OpenAPI。 -->

### 权限、幂等、兼容与发布顺序

<!-- 鉴权、授权、审计、幂等、消费者兼容、迁移和发布顺序。 -->

## 交互设计

### 适用性

<!-- 适用 / 不适用；不适用时说明依据。 -->

### 用户入口与操作路径

<!-- 按 Target 描述入口、操作路径和主要反馈。 -->

### 页面与终端状态

<!-- 覆盖正常、加载、空态、错误、权限和关键边界状态。 -->

## 质量与运行影响

<!-- 性能、安全、并发、一致性、可观测性、部署和配置；无变化时说明依据。 -->

## 关键决策

| 决策 | 选择 | 理由 | 影响 |
| --- | --- | --- | --- |
| <决策点> | <采用方案> | <理由> | <影响范围> |

## 风险与待确认

<!-- 没有则写“无”。未确认事项进入 review.md 跟踪。 -->
