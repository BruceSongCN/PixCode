# <模块中文名>｜<功能中文名>｜API 与共享契约

| 属性 | 内容 |
| --- | --- |
| 模块 | <模块中文名>（`<module-id>`） |
| 功能 | <功能中文名>（`<feature-id>`） |
| Change | `<change-id>` |
| Capability | `<module-id>-<feature-id>` |
| Target | 提供方 `<target-id>`；消费者 `<target-id>` |

## 适用性

<!-- 适用 / 不适用；不适用时说明判断依据。 -->

## 契约清单

| 契约 | 提供方 Target | 消费方 Target | 变化类型 |
| --- | --- | --- | --- |
| `<API / Event / Message>` | `<target-id>` | `<target-id>` | 新增 / 兼容修改 / 破坏性修改 |

## 请求与响应语义

<!-- 字段只记录评审所需的稳定含义、约束和示例，不机械复制生成文档。 -->

## 错误与权限

<!-- 错误码、幂等、鉴权、授权、审计和敏感信息边界。 -->

## 兼容与迁移

<!-- 消费者兼容策略、版本策略、发布顺序和弃用安排。 -->

## 待确认

<!-- 没有则写“无”。 -->
