# <模块中文名>｜<功能中文名>｜模型设计

| 属性 | 内容 |
| --- | --- |
| 模块 | <模块中文名>（`<module-id>`） |
| 功能 | <功能中文名>（`<feature-id>`） |
| Change | `<change-id>` |
| Capability | `<module-id>-<feature-id>` |
| Target | `<target-id>` |

## 适用性

<!-- 适用 / 不适用；不适用时说明判断依据。 -->

## 模型变化

| 类型 | 名称 | 变化 | 所属 Target |
| --- | --- | --- | --- |
| 实体 / 值对象 / 枚举 / DTO | `<name>` | <新增或修改内容> | `<target-id>` |

## 关系与状态

<!-- 可使用 Mermaid 表达关系或状态迁移。 -->

## 规则与不变量

<!-- 唯一性、必填、范围、状态约束、并发约束等。 -->

## 持久化与迁移

<!-- 表、字段、索引、迁移、兼容和数据修复影响。 -->

## 待确认

<!-- 没有则写“无”。 -->
