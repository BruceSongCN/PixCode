# <模块中文名>｜<功能中文名>｜变更提案

| 属性 | 内容 |
| --- | --- |
| 模块 | <模块中文名>（`<module-id>`） |
| 功能 | <功能中文名>（`<feature-id>`） |
| Change | `<module-id>-<feature-id>[-<change-topic>]` |
| Capability | `<module-id>-<feature-id>` |
| Target | `<target-id>` |

## Why

<!-- 为什么要做；当前问题、业务价值和时机。 -->

## What Changes

<!-- 本轮改变什么。破坏性变化使用 **BREAKING** 标记。 -->

## Scope

### In Scope

<!-- 本轮明确包含的范围。 -->

### Out of Scope

<!-- 容易被误解为包含、但本轮明确不做的范围。 -->

## Capabilities

### New Capabilities

<!-- 使用 <module-id>-<feature-id>；每项将生成 specs/<capability>/spec.md。 -->
- `<module-id>-<feature-id>`: <能力说明>

### Modified Capabilities

<!-- 仅列需求语义发生变化的已有 capability；没有则写“无”。 -->
- `<capability>`: <变化说明>

## Targets

| Target | 影响 | 责任边界 |
| --- | --- | --- |
| `<target-id>` | 新增 / 修改 / 无 | <该 Target 承担什么> |

## Impact

<!-- 受影响的代码库、接口、数据、依赖、部署或运维边界。 -->
